import {
  fetchFieldSkills,
  saveFieldSkillLevelUnlocked,
  updateFieldSkillLevelUnlocked
} from '../../../redux/actions/fieldSkills';

export default {
  fetchFieldSkills,
  updateFieldSkillLevelUnlocked,
  saveFieldSkillLevelUnlocked
}