import {
  fetchFieldSkills,
  saveFieldSkillLevelUnlocked,
  updateFieldSkillLevelUnlocked
} from 'reduxState/actions/fieldSkills';

export default {
  fetchFieldSkills,
  updateFieldSkillLevelUnlocked,
  saveFieldSkillLevelUnlocked
}